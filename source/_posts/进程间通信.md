---
title: 进程间通信
date: 2022-05-05 10:11:11
tags: 学习笔记
categories: 网络编程
---

# 进程间通信的基本概念

进程间通信意味着两个不同进程间可以交换数据，为了完成这一点，操作系统中应提供两个进程可以同时访问的内存空间。

<!--more-->

## 通过管道实现进程间通信

管道并非属于进程的资源，而是和套接字一样，属于操作系统。

```c++
#include <unistd.h>

int pipe(int filedes[2]);
//成功返回0，不成功返回-1
```

filedes[0]表示通过管道接收数据时使用的文件描述符，即管道出口

filedes[1]表示通过管道传输数据时使用的文件描述符，即管道入口

> 以长度为2的int数组地址值作为参数调用上述函数时，数组中存有两个文件描述符，它们将被用作管道的出口和入口。父进程调用该函数时将创建管道，同时获取对应于出人口的文件描述符，此时父进程可以读写同一管道。但父进程的目的是与子进程进行数据交换，因此需要将人口或出口中的1个文件描述符传递给子进程。

```c++
#include <stdio.h>
#include <unistd.h>
#define BUF_SIZE 30
int main(int argc, char *argv[]){
	int fds[21;
	char str[]="who are you?";
    char buf[BUF_SIZE];
	pid_t pid;
	pipe(fds);
    pid=fork();
    if(pid==){
		write(fds[1],str, sizeof(str));
    }
     else{
		read(fds[e], buf，BUF_SIZE);
        puts(buf);
	}
	return 0;
}
```

>第12行:调用pipe函数创建管道，fds数组中保存用于I/O的文件描述符。
>第13行:接着调用fork函数。子进程将同时拥有通过第12行函数调用获取的2个文件描述符。注意!复制的并非管道，而是用于管道I/O的文件描述符。至此，父子进程同时拥有I/O文件描述符。
>第16、20行:子进程通过第16行代码向管道传递字符串。父进程通过第20行代码从管道接收字符串。

```c++
#include <stdio.h>
#include <unistd.h>
#define BUF_SIZE 30
int main(int argc, char *argv[]){
	int fds[2];
	char str1[]="Who are you?";
	char str2[]="Thank you for your message";
    char buf[BUF_SIZE];
	pid_t pid;
	pipe(fds);
    pid=fork();
    if(pid==0){
		write(fds[1],str1,sizeof(str1));
        sleep(2);
		read(fds[0]，buf,BUF_SIZE);
		printf("child proc output: %s \n", buf);
}
else{
	read(fds[0],buf,BUF_SIZE);
	printf("Parent proc output: %s ln"， buf);
    write(fds[1]，str2, sizeof(str2));
	sleep(3);
}
return 0;

```



数据进入管道后成为无主数据。也就是通过read函数先读取数据的进程将得到数据，即使该进程将数据传到了管道。因此，注释第18行将产生问题。在第19行，子进程将读回自己在第17行向管道发送的数据。结果，父进程调用read函数后将无限期等待数据进入管道。

































