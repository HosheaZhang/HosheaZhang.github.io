---
title: 有效的括号
date: 2022-04-25 15:33:39
tags: [HOT100, 栈]
categories: 算法
---

# 题目

给定一个只包括 '('，')'，'{'，'}'，'['，']' 的字符串 s ，判断字符串是否有效。

有效字符串需满足：

左括号必须用相同类型的右括号闭合。
左括号必须以正确的顺序闭合。

<!--more-->

# 示例

## 示例1 

```
输入：s = "()"
输出：true
```

## 示例2

```
输入：s = "()[]{}"
输出：true
```

## 示例3

```
输入：s = "(]"
输出：false
```
# 题目提示

- `1 <= s.length <= 104`
- `s` 仅由括号 `'()[]{}'` 组成

# 思考

# 代码

```C++
class Solution {
public:
    bool isValid(string s) {
        int n = s.size();
        if(n%2==1) return false;

        unordered_map<char,char> pairs = {
            {')','('},{']', '['},{'}', '{'}
        };

        stack<char> stk;
        for(char ch:s){
            if(pairs.count(ch)){
                if(stk.empty()||stk.top()!=pairs[ch]){
                    return false;
                }
                stk.pop();
            }
            else stk.push(ch);
        }

        return stk.empty();

    }
};
```

# 总结