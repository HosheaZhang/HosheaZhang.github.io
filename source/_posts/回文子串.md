---
title: 回文子串
date: 2022-05-31 14:56:37
tags: [HOT100, 模拟]
categories: 算法
---

# 题目

给你一个字符串 s ，请你统计并返回这个字符串中 回文子串 的数目。

回文字符串 是正着读和倒过来读一样的字符串。

子字符串 是字符串中的由连续字符组成的一个序列。

具有不同开始位置或结束位置的子串，即使是由相同的字符组成，也会被视作不同的子串。

<!--more-->

# 示例

## 示例1 

```
输入：s = "abc"
输出：3
解释：三个回文子串: "a", "b", "c"
```

## 示例2

```
输入：s = "aaa"
输出：6
解释：6个回文子串: "a", "a", "a", "aa", "aa", "aaa"
```

# 题目提示

- `1 <= s.length <= 1000`
- `s` 由小写英文字母组成

# 思考

# 代码

```C++
class Solution {
public:
    int countSubstrings(string s) {
        int n = s.size(), ans = 0;
        for (int i = 0; i < 2 * n - 1; ++i) {
            int l = i / 2, r = i / 2 + i % 2;
            while (l >= 0 && r < n && s[l] == s[r]) {
                --l;
                ++r;
                ++ans;
            }
        }
        return ans;
    }
};
```

# 总结